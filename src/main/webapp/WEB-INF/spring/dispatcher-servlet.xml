<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Bật annotation-driven (@Controller, @RestController, ...) -->
    <mvc:annotation-driven />

    <!-- Quét package để tìm các bean có annotation -->
    <context:component-scan base-package="com.demo.socialmedia.controller" />

    <!-- Cho phép override cấu hình từ biến môi trường / JVM properties -->
    <context:property-placeholder system-properties-mode="OVERRIDE" />




    <!-- Tự tạo schema + seed dữ liệu demo khi DB trống -->
    <bean id="databasePopulator" class="org.springframework.jdbc.datasource.init.ResourceDatabasePopulator">
        <property name="continueOnError" value="true" />
        <property name="ignoreFailedDrops" value="true" />
        <property name="sqlScriptEncoding" value="UTF-8" />
        <property name="scripts">
            <list>
                <value>classpath:sql/schema.sql</value>
                <value>classpath:sql/data.sql</value>
            </list>
        </property>
    </bean>





    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Cho phép truy cập file tĩnh (HTML, CSS, JS) -->
    <mvc:resources mapping="/static/**" location="/static/" />

</beans>
